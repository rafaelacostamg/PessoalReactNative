import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 20,
  },

  title: {
    fontSize: 30,
    textAlign: 'center',
  },

  bodytext:{
    fontSize: 16,
    textAlign:'center',
  },

  image:{
    width: 300,
    height: 200,
    borderRadius: 15,
  }

}); 